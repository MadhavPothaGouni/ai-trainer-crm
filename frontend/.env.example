# Absolute backend origin, only needed when the frontend is deployed separately
# from the API (prod). In local dev, leave this unset - vite.config.ts proxies
# /api/* to VITE_BACKEND_URL (defaulting to http://localhost:8080) instead.
# VITE_API_BASE_URL=https://api.example.com
